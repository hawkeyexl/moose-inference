---
title: Errors and type index
description: InferenceError, and every type exported from the package root, with the page that documents each.
sidebar:
  order: 8
---

The error class, and a complete index of every exported type with the page that documents it.

## `InferenceError`

```ts
class InferenceError extends Error {
  constructor(message: string);
}
```

`name` is `"InferenceError"`. It is thrown for **operational** failures only:

- a missing API key,
- an unknown provider name,
- an unresolved `"auto"` provider passed to a synchronous factory,
- an unresolved local model selector passed to a synchronous factory,
- `node-llama-cpp` not installed when a local provider is constructed,
- detection finding no usable provider — the message names every provider and why each was
  unavailable.

**A model failure is never thrown.** It is recorded on `run.error`. Two classes, two handling paths —
see [budgets and errors](/moose-inference/extract/budgets-and-errors/).

Catch it at your boundary and translate it into your own error type, or your CLI's exit-code mapping
will not recognise it.

## Type index

Everything below exports from the package root. 36 types, 52 values.

### Provider contract

| Type | Documented in |
|---|---|
| `InferenceProvider` | [Providers](/moose-inference/reference/providers/#the-contract) |
| `CompleteJSONRequest` | [Providers](/moose-inference/reference/providers/#the-contract) |
| `CompleteJSONResponse` | [Providers](/moose-inference/reference/providers/#the-contract) |
| `TokenUsage` | [Providers](/moose-inference/reference/providers/#the-contract) |

### Provider configuration

| Type | Documented in |
|---|---|
| `ProviderSpec` | [Providers](/moose-inference/reference/providers/#providerspec) |
| `ProviderName` | [Providers](/moose-inference/reference/providers/#providerspec) |
| `ProviderSelector` | [Providers](/moose-inference/reference/providers/#providerspec) |
| `ProviderIdentity` | [Providers](/moose-inference/reference/providers/#factories) |
| `AnthropicProviderOptions` | [Providers](/moose-inference/reference/providers/#per-provider-options) |
| `OpenAICompatProviderOptions` | [Providers](/moose-inference/reference/providers/#per-provider-options) |
| `LlamaCppProviderOptions` | [Local models](/moose-inference/reference/local-models/#llamacppprovider) |
| `MockResponse` | [Providers](/moose-inference/reference/providers/#mock) |

### Completion

| Type | Documented in |
|---|---|
| `CompleteValidatedOptions` | [Completion](/moose-inference/reference/completion/#completevalidatedoptions) |
| `InferenceRun` | [Completion](/moose-inference/reference/completion/#inferencerun) |

### Judge

| Type | Documented in |
|---|---|
| `EnsembleOptions` | [Judge](/moose-inference/reference/judge/#ensembleoptions) |
| `JudgeRun` | [Judge](/moose-inference/reference/judge/#judgerun-and-judgeverdict) |
| `JudgeVerdict` | [Judge](/moose-inference/reference/judge/#judgerun-and-judgeverdict) |
| `ConsensusResult` | [Judge](/moose-inference/reference/judge/#computeconsensus) |
| `Match` | [Judge](/moose-inference/reference/judge/#judgerun-and-judgeverdict) |
| `Zone` | [Judge](/moose-inference/reference/judge/#zonefor) |
| `ZoneThresholds` | [Judge](/moose-inference/reference/judge/#zonefor) |

### Cost

| Type | Documented in |
|---|---|
| `Pricing` | [Cost](/moose-inference/reference/cost/#pricing) |

### Subprocess

| Type | Documented in |
|---|---|
| `ExecFn` | [Exec](/moose-inference/reference/exec/#execfn) |
| `ExecOptions` | [Exec](/moose-inference/reference/exec/#execoptions) |
| `ExecResult` | [Exec](/moose-inference/reference/exec/#execresult) |

### Local models

| Type | Documented in |
|---|---|
| `LlamaModelEntry` | [Local models](/moose-inference/reference/local-models/#the-catalog) |
| `LlamaTier` | [Local models](/moose-inference/reference/local-models/#selectors-and-tiers) |
| `LlamaSelector` | [Local models](/moose-inference/reference/local-models/#selectors-and-tiers) |
| `LlamaRuntime` | [Local models](/moose-inference/reference/local-models/#the-runtime-seam) |
| `LlamaLoadedModel` | [Local models](/moose-inference/reference/local-models/#the-runtime-seam) |
| `LlamaSession` | [Local models](/moose-inference/reference/local-models/#the-runtime-seam) |
| `LlamaPromptOptions` | [Local models](/moose-inference/reference/local-models/#the-runtime-seam) |
| `LlamaPromptResult` | [Local models](/moose-inference/reference/local-models/#the-runtime-seam) |
| `ClearLlamaModelsOptions` | [Local models](/moose-inference/reference/local-models/#model-files-on-disk) |
| `ClearLlamaModelsResult` | [Local models](/moose-inference/reference/local-models/#model-files-on-disk) |
| `ClearedModelFile` | [Local models](/moose-inference/reference/local-models/#model-files-on-disk) |

`JsonCache` is a class, not a type — see [Cache](/moose-inference/reference/cache/#jsoncache).

## Source of truth

`src/types.ts` for `InferenceError`; `src/index.ts` for the export surface.
