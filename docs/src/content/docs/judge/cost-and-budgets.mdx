---
title: Cost and budgets
description: Price a run correctly, and learn the difference between a model that costs zero and one that cannot be priced.
sidebar:
  order: 3
---

import { Aside, Code } from '@astrojs/starlight/components';
import costBudget from '@examples/cost-budget.mjs?raw';

Price a run, and know exactly when the number can be trusted.

The price table and full signatures are in the [cost reference](/moose-inference/reference/cost/).

## The rule

> **Unknown model price is `undefined`. Unknown cost is `0`.** The library never guesses.

A fabricated price is worse than an absent one when a budget gate depends on it. So there is no
fallback estimate, no "average model" default, and no interpolation.

That rule is correct. Its consequence is the thing that has actually cost people money.

## The trap

<Aside type="danger" title="A gate over an unpriced model is inert, not satisfied">
  If a model is not in `PRICE_TABLE` and you supply no override, every `maxCostUsd` check evaluates
  against `0` and **passes unconditionally**. Your budget is not being respected — it is not being
  applied at all.
</Aside>

There are two very different situations, and they look identical from inside a comparison:

| Situation | `pricingFor` returns | Cost | Meaning |
|---|---|---|---|
| A cached replay | a price | `0` | **Costs zero.** Genuinely free. |
| A local model | `undefined` | `0` | **Cannot be priced.** No per-token cost exists. |
| `claude-cli` | a price, but no usage | `0` | **Cannot be measured.** The provider reports no tokens. |
| An unknown hosted model | `undefined` | `0` | **Cannot be priced.** Your gate is inert. |

Only the first row is a real zero. Check `pricingFor(...) === undefined` before trusting a gate.

## Price a run

<Code code={costBudget} lang="js" title="examples/cost-budget.mjs" />

```text
known model: claude-sonnet-4-5
  pricing: { inputPerMTok: 3, outputPerMTok: 15 }
  cost usd: 0.009000
pinned variant: { inputPerMTok: 3, outputPerMTok: 15 }
unknown model: some-new-model-v1
  pricing: undefined
  cost usd: 0.000000
  budget gate passes: true — but only because the price is unknown
  gate is inert: true
  with override: 0.005400 usd
```

## How a price is resolved

`pricingFor(model, override?)` tries, in order:

1. **The explicit override**, if you passed one.
2. **An exact match** in `PRICE_TABLE`.
3. **The longest matching prefix.**

Longest, not first — so a pinned variant like `claude-sonnet-4-5-20250929` resolves to its family
price, and adding a longer, more specific entry later can never be shadowed by a shorter one.

If none match, the result is `undefined`.

## Supply a price you know

Two equivalent routes, depending on whether you have a spec or a model name to hand:

```ts
// On the spec — one object serves both construction and pricing.
const spec = { provider: "openai", model: "my-model", pricing: { inputPerMTok: 2, outputPerMTok: 8 } };

// Or directly, as the override argument.
const pricing = pricingFor(model, config.pricing);
```

If your users can configure a model, let them configure its price too. Otherwise every model you
have not enumerated silently disables their budget.

## What is never charged

- **Cached runs.** `costOfRuns` skips anything flagged `cached`.
- **Runs with no `usage`.** `claude-cli` reports none at all.
- **Local models.** No table entry, by design.
- **The failed attempt of a retried request.**

<Aside type="caution" title="Your ceiling is soft">
  `InferenceRun` carries only the *successful* attempt's usage. A request that failed validation on
  the first attempt and succeeded on the retry is charged for one attempt, not two — so the failed
  attempt's input tokens never reach your gate. Leave headroom.
</Aside>

## Gate before, not after

Check the accumulated cost *before* dispatching the next ensemble. Checking afterwards means the
overspend has already happened.

```ts
let spent = 0;
const pricing = pricingFor(model, config.pricing);

for (const subject of subjects) {
  if (pricing !== undefined && spent >= config.maxCostUsd) {
    warn(`budget of $${config.maxCostUsd} reached; ${remaining} subjects skipped`);
    break;
  }
  const runs = await runEnsemble({ provider, system, user: subject.body, runs: 3 });
  spent += costOfRuns(runs, pricing);
}
```

The `pricing !== undefined` guard is what stops an unpriced model from turning the loop into an
unbounded one. Without it, the gate is inert — see above.

## Next

- [Caching](/moose-inference/judge/caching/) — the cheapest call is the one you do not make
- [Cost reference](/moose-inference/reference/cost/) — the full price table and signatures

{/* test {"testId":"judge-cost","description":"Pricing resolution, prefix matching, and the inert-gate warning."} */}
{/* step {"description":"A known model prices from the built-in table and a pinned variant resolves by longest prefix (1/2)","runCode":{"language":"javascript","code":"process.stdout.write(require('node:child_process').execFileSync('node',['examples/cost-budget.mjs'],{encoding:'utf8',stdio:['ignore','pipe','inherit']}))","stdio":"pricing: { inputPerMTok: 3, outputPerMTok: 15 }"}} */}
{/* step {"description":"A known model prices from the built-in table and a pinned variant resolves by longest prefix (2/2)","runCode":{"language":"javascript","code":"process.stdout.write(require('node:child_process').execFileSync('node',['examples/cost-budget.mjs'],{encoding:'utf8',stdio:['ignore','pipe','inherit']}))","stdio":"pinned variant: { inputPerMTok: 3, outputPerMTok: 15 }"}} */}
{/* step {"description":"An unknown model prices as undefined rather than as a guess.","runCode":{"language":"javascript","code":"process.stdout.write(require('node:child_process').execFileSync('node',['examples/cost-budget.mjs'],{encoding:'utf8',stdio:['ignore','pipe','inherit']}))","stdio":"pricing: undefined"}} */}
{/* step {"description":"A budget gate over an unpriced model is inert, and an override makes it real again (1/2)","runCode":{"language":"javascript","code":"process.stdout.write(require('node:child_process').execFileSync('node',['examples/cost-budget.mjs'],{encoding:'utf8',stdio:['ignore','pipe','inherit']}))","stdio":"gate is inert: true"}} */}
{/* step {"description":"A budget gate over an unpriced model is inert, and an override makes it real again (2/2)","runCode":{"language":"javascript","code":"process.stdout.write(require('node:child_process').execFileSync('node',['examples/cost-budget.mjs'],{encoding:'utf8',stdio:['ignore','pipe','inherit']}))","stdio":"with override: 0.005400 usd"}} */}
{/* test end */}
