---
title: Judge & consensus
description: Turn N independent model opinions into one decision your CLI can act on, with consensus math and confidence zones.
sidebar:
  order: 1
---

import { Aside, Code } from '@astrojs/starlight/components';
import judgeEnsemble from '@examples/judge-ensemble.mjs?raw';

Run the same question past a model N times and turn the answers into a decision you can defend.

This page covers the ensemble, the consensus math, and the zone routing.
[Caching](/moose-inference/judge/caching/) and [budgets](/moose-inference/judge/cost-and-budgets/) build on it.

## The promise first

Everything on this page exists to deliver one guarantee:

> **Only a unanimous, high-confidence ensemble auto-resolves.** Anything split, low-confidence, or
> containing an errored run routes to `human-review`.

An errored run can push a result toward review. It can never produce a silent pass. If you are
building a product promise on top of this — and you probably are, because "the model said so" is not
an answer your users will accept — that is the sentence to build it on.

## Run one

`judge()` does the whole thing: N runs, consensus, and zone routing in one call.

<Code code={judgeEnsemble} lang="js" title="examples/judge-ensemble.mjs" />

```text
verdict: pass
zone: auto-pass
votes: { pass: 3, fail: 0, partial: 0, error: 0 }
agreement: 1.00
meanConfidence: 0.95
degraded.verdict: pass
degraded.zone: human-review
degraded.votes: { pass: 2, fail: 0, partial: 0, error: 1 }
errored run forced review: true
```

The second half is the guarantee, demonstrated. Two runs still passed confidently — and the result
did **not** auto-resolve, because one run errored.

<Aside type="note" title="Why two scripted errors, not one">
  Each run retries once before giving up, so a single scripted failure is absorbed by the retry and
  never reaches the consensus. Two consecutive failures exhaust one run's attempts and produce a
  genuinely errored run. This is worth knowing when you write your own failure tests.
</Aside>

## Read the result

`ConsensusResult` is what you act on:

| Field | Meaning |
|---|---|
| `zone` | `auto-pass`, `auto-fail`, or `human-review` — **the field you branch on** |
| `verdict` | `pass` or `fail` — the binary answer |
| `votes` | `{ pass, fail, partial, error }` — the full breakdown |
| `agreement` | `0`–`1` across non-errored runs |
| `meanConfidence` | mean over non-errored runs |
| `runs` | every `JudgeRun`, for logging and cost accounting |

Branch on `zone`. Use `agreement` and `meanConfidence` afterwards, to explain the decision to
whoever asks.

## The exact boundaries

These are guarantees, so they are stated precisely rather than approximately.

- **`partial` counts toward fail** for the binary verdict, but stays visible in `votes`.
- **A tie is not a pass.** `auto-pass` requires `pass > 0`, and zero `fail`, `partial`, and `error`
  votes.
- **`auto-fail` requires zero passes** and zero errors, with at least one `fail` or `partial`.
- **Any errored run forces `human-review`**, regardless of how confident the others were.
- **`agreement` and `meanConfidence` cover non-errored runs only.** All-errored yields `0`
  agreement.
- Both zones also require `meanConfidence` to clear their threshold.

## Tune the thresholds

```ts
const consensus = await judge({
  provider, system, user, runs: 3,
  zones: { autoPass: 0.9, autoFail: 0.75 },
});
```

`DEFAULT_ZONES` is `{ autoPass: 0.8, autoFail: 0.8 }`. Raising a threshold sends more results to
review; lowering it sends fewer.

What thresholds **cannot** do is override the vote rules. A split ensemble goes to review at any
threshold, and so does one containing an error. Confidence is a second gate, not a substitute for
agreement.

## When you need the runs

`judge()` is `runEnsemble` + `computeConsensus` + `zoneFor`. Call them separately when you need the
individual runs in between — to cache them, to price them, or to log each verdict.

```ts
import { computeConsensus, runEnsemble, zoneFor } from "moose-inference";

const runs = await runEnsemble({ provider, system, user, runs: 3 });
const base = computeConsensus(runs);
const zone = zoneFor(base, { autoPass: 0.9, autoFail: 0.8 });
```

<Aside type="caution" title="Runs are sequential">
  `runs: 5` means five calls one after another, not five in parallel. Concurrency belongs one level
  up — across subjects, where you control it — rather than inside a single ensemble.
</Aside>

## Temperature

Defaults to `0`. Raising it warns once per process, because independent samples at temperature zero
are already independent: the provider is called separately each time, with no shared state. If you
want diversity, prefer more runs over more randomness.

## Next

- Stop paying for unchanged subjects → [Caching](/moose-inference/judge/caching/)
- Keep spend inside a ceiling → [Cost and budgets](/moose-inference/judge/cost-and-budgets/)
- Word the verdict in your own domain → [Custom verdict schema](/moose-inference/judge/verdict-schema/)
- Test all of it offline → [Testing your integration](/moose-inference/keep-it-working/testing/)

{/* test {"testId":"judge-ensemble","description":"The anchor journey: ensemble, consensus, zones, and the errored-run safety property."} */}
{/* step {"description":"A unanimous high-confidence ensemble auto-resolves with full agreement (1/3)","runCode":{"language":"javascript","code":"process.stdout.write(require('node:child_process').execFileSync('node',['examples/judge-ensemble.mjs'],{encoding:'utf8',stdio:['ignore','pipe','inherit']}))","stdio":"verdict: pass"}} */}
{/* step {"description":"A unanimous high-confidence ensemble auto-resolves with full agreement (2/3)","runCode":{"language":"javascript","code":"process.stdout.write(require('node:child_process').execFileSync('node',['examples/judge-ensemble.mjs'],{encoding:'utf8',stdio:['ignore','pipe','inherit']}))","stdio":"zone: auto-pass"}} */}
{/* step {"description":"A unanimous high-confidence ensemble auto-resolves with full agreement (3/3)","runCode":{"language":"javascript","code":"process.stdout.write(require('node:child_process').execFileSync('node',['examples/judge-ensemble.mjs'],{encoding:'utf8',stdio:['ignore','pipe','inherit']}))","stdio":"agreement: 1.00"}} */}
{/* step {"description":"An errored run forces human-review even when the remaining runs pass confidently (1/2)","runCode":{"language":"javascript","code":"process.stdout.write(require('node:child_process').execFileSync('node',['examples/judge-ensemble.mjs'],{encoding:'utf8',stdio:['ignore','pipe','inherit']}))","stdio":"degraded.zone: human-review"}} */}
{/* step {"description":"An errored run forces human-review even when the remaining runs pass confidently (2/2)","runCode":{"language":"javascript","code":"process.stdout.write(require('node:child_process').execFileSync('node',['examples/judge-ensemble.mjs'],{encoding:'utf8',stdio:['ignore','pipe','inherit']}))","stdio":"errored run forced review: true"}} */}
{/* step {"description":"The errored run is visible in the vote breakdown rather than silently dropped.","runCode":{"language":"javascript","code":"process.stdout.write(require('node:child_process').execFileSync('node',['examples/judge-ensemble.mjs'],{encoding:'utf8',stdio:['ignore','pipe','inherit']}))","stdio":"degraded.votes: { pass: 2, fail: 0, partial: 0, error: 1 }"}} */}
{/* test end */}
