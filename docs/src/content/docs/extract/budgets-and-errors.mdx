---
title: Budgets and errors
description: Keep an optional feature inside a cost ceiling, and make every failure land as a warning rather than a stack trace.
sidebar:
  order: 2
---

import { Aside, Code } from '@astrojs/starlight/components';
import budgetAndDegrade from '@examples/budget-and-degrade.mjs?raw';

Bounding what an optional feature can spend, and making sure nothing it does can take
down the deterministic tool it lives inside.

The pricing rules themselves are [cost and budgets](/moose-inference/judge/cost-and-budgets/); this page
is about wiring them into a CLI that has to keep working.

## Two failure classes

The distinction everything here rests on, and the one most often collapsed:

| Class | How it arrives | Examples | Correct handling |
|---|---|---|---|
| **Operational** | thrown as `InferenceError` | missing API key, unknown provider name, unresolved selector | Catch at the boundary, translate, exit with your config-error code |
| **Model** | returned on `run.error` | validation failed after both attempts, provider API error, timeout | Never thrown. Record it, skip the subject, carry on |

Collapse them and a fixable configuration mistake becomes an unhandled stack trace with the wrong
exit code.

## Do it

<Code code={budgetAndDegrade} lang="js" title="examples/budget-and-degrade.mjs" />

```text
translated: true
message: config: Anthropic provider needs EXAMPLE_KEY_T
errors.md: skipped — Response failed schema validation:  mu
budget reached at $0.009000 — skipping retries.md
filled: 3 skipped: 2
spent usd: 0.009000 ceiling usd: 0.009000
calls actually made: 5
calls charged to the total: 3
```

## Translate `InferenceError` at the boundary

```ts
function buildProvider(spec) {
  try {
    return makeProvider(spec);
  } catch (error) {
    if (error instanceof InferenceError) throw new MyToolError(`config: ${error.message}`);
    throw error;
  }
}
```

Your `fail()` handler maps *your* error type to an exit code. A foreign type escapes it and becomes
a crash. Every existing consumer of this package wrote this same six-line boundary — do not
rediscover it.

Keep the rethrow for anything that is not an `InferenceError`. Swallowing unknown errors is how a
real bug becomes a silent no-op.

## Gate before the call, not after

```ts
if (pricing !== undefined && spent >= config.maxCostUsd) break;
```

Checking afterwards means the overspend has already happened.

<Aside type="danger" title="The pricing guard is not optional">
  Without `pricing !== undefined`, an unpriced model makes `spent` permanently `0` and the loop
  becomes unbounded. The gate is not satisfied — it is
  [inert](/moose-inference/judge/cost-and-budgets/#the-trap).
</Aside>

## Your ceiling is soft, in three ways

The last two lines of the sample output are the point: **five calls were made, three were charged.**

1. **Gating before each call bounds the overshoot to one call — while the loop is sequential.** It
   does not prevent one: the call that crosses the line has already been paid for.
2. **A retried request reports only the successful attempt's usage.** The two rejected attempts on
   `errors.md` cost real input tokens that never reached the total.
3. **Concurrency widens the bound.** The loop above is sequential, so exactly one call can be in
   flight when the gate opens. Run subjects concurrently — which
   [we recommend for ensembles](/moose-inference/judge/at-scale/) — and up to *K* calls can clear the gate
   before any of them adds to `spent`. The overshoot becomes K calls, not one.

<Aside type="caution" title="Rule 1 and concurrency do not compose">
  The one-call bound is a property of sequential iteration, not of the gate. If you add a worker
  pool, recompute your headroom against the pool size. See
  [running at scale](/moose-inference/judge/at-scale/#the-budget-gate-under-concurrency).
</Aside>

Leave headroom. Treat the number as a bound with slack, not a measurement.

## Degrade, do not fail

```ts
if (run.error !== undefined) {
  warn(`${doc.path}: skipped — ${run.error}`);
  continue;            // the deterministic path is untouched
}
```

Three situations must all end in a warning and a clean exit, never a stack trace:

| Situation | What happens | Why it is safe |
|---|---|---|
| No API key | `InferenceError` at construction | Translated, reported as configuration |
| Read-only cache directory | Warns once, run continues | The cache is an optimization, never a dependency |
| Budget exhausted | Loop breaks, remaining subjects skipped | Reported in the summary, exit code unchanged |
| Model returns nothing usable | `run.error` set, subject skipped | Never coerced into your output |

That last row is the invariant the whole layer is built on: **an errored run is recorded, never
dropped and never coerced.**

## Next

- [Cost and budgets](/moose-inference/judge/cost-and-budgets/) — pricing resolution and the price table
- [Cost reference](/moose-inference/reference/cost/) — signatures
- [Errors and type index](/moose-inference/reference/errors-and-types/) — everything `InferenceError` covers

{/* test {"testId":"budgets-and-errors","description":"Error translation, gating before the call, and the two ways the ceiling is soft."} */}
{/* step {"description":"An operational failure is translated into the host CLI's own error type.","runCode":{"language":"javascript","code":"process.stdout.write(require('node:child_process').execFileSync('node',['examples/budget-and-degrade.mjs'],{encoding:'utf8',stdio:['ignore','pipe','inherit']}))","stdio":"translated: true"}} */}
{/* step {"description":"A model failure is recorded and the document skipped, leaving the run alive.","runCode":{"language":"javascript","code":"process.stdout.write(require('node:child_process').execFileSync('node',['examples/budget-and-degrade.mjs'],{encoding:'utf8',stdio:['ignore','pipe','inherit']}))","stdio":"errors.md: skipped"}} */}
{/* step {"description":"The budget gate stops further work once the ceiling is reached.","runCode":{"language":"javascript","code":"process.stdout.write(require('node:child_process').execFileSync('node',['examples/budget-and-degrade.mjs'],{encoding:'utf8',stdio:['ignore','pipe','inherit']}))","stdio":"budget reached at $0.009000"}} */}
{/* step {"description":"A retried request under-reports: five calls made, three charged.","runCode":{"language":"javascript","code":"process.stdout.write(require('node:child_process').execFileSync('node',['examples/budget-and-degrade.mjs'],{encoding:'utf8',stdio:['ignore','pipe','inherit']}))","stdio":"calls actually made: 5"}} */}
{/* test end */}
